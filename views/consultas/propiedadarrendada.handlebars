<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Propiedades Arrendadas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

    <section class="bg-dark text-white text-center py-5">
        <div class="container">
            <h1 class="display-4">Propiedades Arrendadas</h1>
            <p class="lead">Aquí podrás visualizar el estado de arrendamiento de las propiedades.</p>
            <a href="/" class="btn btn-lg btn-secondary">Volver al Inicio</a>
        </div>
    </section>

    <section class="container my-5">
        <h2 class="text-center mb-4">Listado de Propiedades</h2>

        <table class="table table-bordered table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th class="text-center">ID Propiedad</th>
                    <th class="text-center">Tipo</th>
                    <th class="text-center">Número</th>
                    <th class="text-center">Estado</th>
                </tr>
            </thead>
            <tbody>
                {{#each propiedadesarrendadas}}
                <tr>
                    <td class="text-center">{{this.idpropiedad}}</td>
                    <td class="text-center"> {{this.tipo}}</td>
                    <td class="text-center">{{this.numero_propiedad}}</td>
                    <td class="text-center">
                        {{#if this.propiedadarrendada}}
                        <span class="badge bg-success">Arrendado</span>
                        {{else}}
                        <span class="badge bg-danger">No Arrendado</span>
                        {{/if}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
