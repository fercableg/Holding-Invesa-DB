<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ver Arrendatarios</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

    <section class="bg-dark text-white text-center py-5">
        <div class="container">
            <h1 class="display-4">Tabla de Arrendatarios</h1>
            <p class="lead">
                Aquí podrás ver todos los registros de la tabla Arrendatarios.
            </p>
            <a href="/arrendatario" class="btn btn-lg btn-secondary">
                Volver a Arrendatarios
            </a>
        </div>
    </section>

    <section class="container my-5">
        <h2 class="text-center mb-4">Listado de Arrendatarios</h2>

        <table class="table table-bordered table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th class="text-center">ID Arrendatario</th>
                    <th class="text-center">ID Propiedad</th>
                    <th class="text-center">Nombre</th>
                    <th class="text-center">RUT</th>
                    <th class="text-center">Telefono</th>
                    <th class="text-center">Correo</th>
                    <th class="text-center">Direccion</th>
                </tr>
            </thead>
            <tbody>
                {{#each verarrendatarios}}
                    <tr>
                        <td>{{this.idarrendatario}}</td>
                        <td>{{this.idpropiedad}}</td>
                        <td>{{this.nombre}}</td>
                        <td>{{this.rut}}</td>
                        <td>{{this.telefono}}</td>
                        <td>{{this.correo}}</td>
                        <td>{{this.direccion}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
